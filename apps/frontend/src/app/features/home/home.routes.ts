import { Routes } from '@angular/router';

import { HomeComponent } from 'app/features/home/pages/home/home.component';

export const routes: Routes = [
  { title: 'Index', path: '', component: HomeComponent },
];
